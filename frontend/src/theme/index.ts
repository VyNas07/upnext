// Tema padrão do Chakra UI v2
// As cores personalizadas serão aplicadas diretamente nos componentes
export const theme = {
    // Chakra UI v2 usa tema padrão
    // Cores personalizadas podem ser aplicadas via props dos componentes
};
